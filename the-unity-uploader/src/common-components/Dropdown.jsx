import {createRoot} from 'react-dom/client';
import {createElement} from 'react';
import './Dropdown.css'

function Dropdown(props) {
    const dropperName = props.name;
    const childDroppers = props.children; //map of an array, which has maps in it

    // function getChildDroppers() {
    //     for (var i = 0; i < childDroppers.length; i++) {
            
    //     }
    // }

    // return createElement(
    //     'elem',
    //     undefined,
    //      
    // );
}

export default Dropdown;